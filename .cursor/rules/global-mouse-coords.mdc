---
description: 【强制】dyclicker 全局鼠标坐标默认值须与 rules 同步
alwaysApply: true
---

# 全局鼠标坐标默认值（强制约定）

## 权威来源与同步链

凡涉及鼠标点击坐标的代码或配置，**必须**保持下列一致：

| 优先级 | 位置 | 说明 |
|--------|------|------|
| 1 | `.cursor/rules/global-mouse-coords.mdc` | 文档权威来源 |
| 2 | `.cursor/rules/dyclicker.mdc` §各阶段坐标 | 功能说明交叉引用 |
| 3 | 代码内 `REFERENCE_*` 常量 | 运行时参考 |
| 4 | 默认配置文件 | 新装模板 |

**禁止**在默认配置或 `REFERENCE_*` 中留 `{x: 0, y: 0}` 占位（未校准的临时值除外）。

## 全局功能按钮

| 按钮 | 坐标 (x, y) |
|------|-------------|
| back（回退） | (442, 955) |
| record（录音） | (670, 955) |
| listen（收听） | (700, 955) |
| replay（重播） | (910, 955) |
| forward（前进） | (1000, 955) |

布局规律：y 固定 955；自左至右为 back → record → listen → … → replay → forward。

## Step1 各 workflow 槽位

6 个 slot 坐标（所有 workflow 的 step1 页面共用同一组物理位置）：

| Slot | 坐标 (x, y) | wf step1 使用 |
|------|-------------|---------------|
| slot1 | (525, 420) | wf1 |
| slot2 | (955, 415) | wf2 |
| slot3 | (525, 530) | wf3 |
| slot4 | (955, 530) | wf4 |
| slot5 | (525, 655) | wf5 |
| slot6 | (955, 650) | wf6 |

布局规律：左列 x≈525（slot1/3/5），右列 x≈955（slot2/4/6）；y 自上而下递增。

## Step2 共享槽位（6 点）

所有 workflow 的 step2 页面共用：

| Slot | 坐标 (x, y) | dyclicker 使用 |
|------|-------------|----------------|
| slot1 | (570, 343) | |
| slot2 | (570, 450) | |
| slot3 | (570, 547) | |
| slot4 | (570, 636) | |
| slot5 | (570, 745) | |
| slot6 | (570, 837) | **step2 点击** |

布局规律：x 固定 570；y 自上而下递增。

## Step3 共享槽位（6 点）

所有 workflow 的 step3 页面共用：

| Slot | 坐标 (x, y) | dyclicker 使用 |
|------|-------------|----------------|
| slot1 | (980, 343) | |
| slot2 | (980, 450) | |
| slot3 | (980, 547) | |
| slot4 | (980, 636) | |
| slot5 | (980, 745) | |
| slot6 | (980, 837) | **step3 点击 + 静音续播点击** |

布局规律：x 固定 980；y 与 step2 各槽位对齐（343 / 450 / 547 / 636 / 745 / 837）。

## 建议 config 结构

```yaml
global_buttons:
  back: {x: 442, y: 955}
  record: {x: 670, y: 955}
  listen: {x: 700, y: 955}
  replay: {x: 910, y: 955}
  forward: {x: 1000, y: 955}

workflows:
  step1_slots:
    - {x: 525, y: 420}   # slot1 → wf1 step1
    - {x: 955, y: 415}   # slot2 → wf2 step1
    - {x: 525, y: 530}   # slot3 → wf3 step1
    - {x: 955, y: 530}   # slot4 → wf4 step1
    - {x: 525, y: 655}   # slot5 → wf5 step1
    - {x: 955, y: 650}   # slot6 → wf6 step1
  step2_shared:
    - {x: 570, y: 343}
    - {x: 570, y: 450}
    - {x: 570, y: 547}
    - {x: 570, y: 636}
    - {x: 570, y: 745}
    - {x: 570, y: 837}   # step2 点击 slot6
  step3_shared:
    - {x: 980, y: 343}
    - {x: 980, y: 450}
    - {x: 980, y: 547}
    - {x: 980, y: 636}
    - {x: 980, y: 745}
    - {x: 980, y: 837}   # step3 点击 + 静音续播 slot6
```

## 四向同步约束

1. 改 rules 坐标时：**先改本文件 → `REFERENCE_*` → 默认配置 → 相关测试**。
2. step2/step3 默认均点击 **slot6**（索引 5）。
3. step1 按 workflow 编号 N 点击 **slotN**（索引 N-1）。
4. **勿随意改动上述参考坐标**，除非用户明确要求或 Dy 界面布局变更后重新矫正。

## 每次改代码检查清单

- [ ] `REFERENCE_*` 与本文件表格一致
- [ ] 默认配置已更新
- [ ] runner / GUI 无与参考坐标冲突的硬编码
- [ ] 用户已校准的坐标不会被未经请求的全量覆盖
